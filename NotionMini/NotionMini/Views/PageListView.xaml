<UserControl x:Class="NotionMini.Views.PageListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">
    <DockPanel Margin="8">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <TextBlock Text="Pages" FontWeight="Bold" Margin="0,0,12,0" VerticalAlignment="Center"/>
            <TextBox Width="200" Margin="0,0,8,0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Search" Command="{Binding ApplySearchCommand}" Margin="0,0,8,0"/>
            <Button Content="+ New Page" Command="{Binding AddPageCommand}"/>
        </StackPanel>

        <ListBox ItemsSource="{Binding Pages}"
                 SelectedItem="{Binding Selected, Mode=TwoWay}"
                 DisplayMemberPath="Title"
                 Margin="0,8,0,0"/>
    </DockPanel>
</UserControl>
